spring:
  application:
    name: IdpContract
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.207:8848
      config:
        server-addr: 192.168.1.207:8848
  profiles:
    active: ${profile:dev}

server:
  port: ${port:8081}

#基本信息
info:
  application: contract
  description: contract middleground<zhongtai>
  version: v1.0
  author: yandeyu